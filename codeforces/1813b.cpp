#include <bits/stdc++.h>
using namespace std; 
typedef long long ll

void solve() {
    ll n;
    cin >> n;
    vector<int> a,b;
    for(int i {};i<n;i++){
        int f;cin>>f;
        a[i] = f;
    }
    for(int i {};i<n;i++){
        int f;cin>>f;
        b[i] = f;
    }
    
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin >> t;
    while(t--) {
        solve();
    }
    return 0;
}