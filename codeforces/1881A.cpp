/*
what i will do is i will remove one letter from the front and put it at the end and then check it the other string is a substring of the new formed string 

*/
#include <iostream>
#include <vector>
#include <string>
using namespace std;



int main(){
    int t;
    cin >> t;
    while(t){
        string x{}, s{};
        getline(cin >> ws, x);
        getline(cin >> ws, s);
        int n {x.length()};
        int m {s.length()};
        if(m<n){
            int maxl = 
        }
        for(int i{} ;i <= n;i++){
            
        }
    }    


}