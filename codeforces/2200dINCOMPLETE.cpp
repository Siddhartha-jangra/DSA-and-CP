#include <iostream>
#include <vector>
using namespaces std;

int main(){
    int testcases{};
    cin >> testcases{};
    while(testcases){
        vector<int> nums;
        int n,x,y;
        cin >> n >> x >> y;
        while(n){
            int val;
            cin >> val;
            nums.push_back(val);
        }
        //i will check the value of the right side of the portals to check if the left portal has lower value and swap
        //how can the loop be run in this question...
        while()
    }
}