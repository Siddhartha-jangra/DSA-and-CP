void selection(vector<int> &nums){
    int i {};
    int j {nums.size() -1};

    while(i < j +1){
        while(j > i){
            if(nums[i] < nums[j]){
                swap(nums[i], nums[j]);
            }
        }
    }
}
