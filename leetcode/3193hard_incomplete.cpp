#include <iostream>
#include <vector>
using namespace std;
long long answer(vector<int> &nums){
/*brute force*/
    int count{};
    auto i = nums.rbegin();
    while(i != nums.rend()){
        for()
    }
}

int main(){
    vector<int> nums = {3,5,2,5,2};
    
 cout << answer(nums);
}